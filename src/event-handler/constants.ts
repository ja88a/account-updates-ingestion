/** Max number of leaders (account with max tokens) to keep track of & report on, per account type */
export const LEADERBOARD_LIST_SIZE = 3;

/** Functional buffer used to managed the list of leaders */
export const LEADERBOARD_SIZE_BUFFER = 2;

/** The maximum number of entries to be kept in memory about the history of each account type's top owner */
export const TOP_OWNERS_HISTORY_MAX_SIZE = 200;
